---
interface Props {
    title: string;
    description: string;
}

const { title, description } = Astro.props satisfies Props;
---

<h3>{title}</h3>
<p>{description}</p>

<style>
    h3  + p {
       margin: 1rem 0;
       color: var(--sub-text);
    }
</style>